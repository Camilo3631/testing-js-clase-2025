name: 💅 Lint
on: push

defaults:
  run:
    working-directory: ./api  # ajusta según dónde esté package.json

jobs: 
  lint:
    runs-on: macos-latest
    steps:
      - name: 👍 Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📥 Install Dependencies
        run: npm ci

      - name: 🌹 Run ESLint
        run: npm run lint
